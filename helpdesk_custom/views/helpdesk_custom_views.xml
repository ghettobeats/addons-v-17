<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="helpdesk_extended_view_form" model="ir.ui.view">
        <field name="name">helpdesk_extended_view_form</field>
        <field name="model">helpdesk.ticket</field>
        <field name="inherit_id" ref="helpdesk.helpdesk_ticket_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group/group/field[@name='ticket_type_id']" position="after">
                <field name="official_document_date"/>
            </xpath>
            <xpath expr="//group/group[2]" position="inside">
                <field name="requesting_area"/>
                <field name="beneficiary"/>
                <field name="signature_missing" widget="boolean_toggle" readonly="1"/>
                <field name="related_invoices" widget="many2many_tags" domain="[('state', '=', 'posted')]"/>
                <field name="related_payments" widget="many2many_tags" domain="[('move_id.state', '=', 'posted')]"/>
                <field name="ticket_end_date" readonly="1"/>
            </xpath>
            <xpath expr="//field[@name='description']" position="before">
                <field name="stage_log_ids" mode="tree" invisible="0">
                    <tree create="0" editable="bottom" delete="0">
                        <field name="stage_id" readonly="1"/>
                        <field name="write_uid" string="Modificado por" readonly="1"/>
                        <field name="assign_user" readonly="1"/>
                        <field name="signature" string="Firma" widget="signature"/>
                        <field name="stage_start_date" widget="1" readonly="1"/>
                        <field name="stage_end_date" widget="1" readonly="1"/>
                        <field name="stage_time_spent" widget="float_time" readonly="1" force_save="1"/>
                    </tree>
                </field>
            </xpath>
            <xpath expr="//field[@name='description']" position="attributes">
                <attribute name="placeholder">Descripción</attribute>
            </xpath>
        </field>
    </record>
</odoo>